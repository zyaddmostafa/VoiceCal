class WeightEntry {
  final double weight;
  final DateTime date;
  final bool isSelected;

  const WeightEntry({
    required this.weight,
    required this.date,
    required this.isSelected,
  });
}
